#!/bin/bash

DIR="./generated/schema.prisma"

if [ -f "$DIR" ]; then

  echo "@puq/auth-db prisma client already exists"

else

  echo "Generating @puq/auth-db prisma client"

  npx prisma migrate dev --name updated

  npx prisma generate
  
fi
