{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "date property options",
  "properties": {
    "type": {
      "const": "date"
    },
    "minDate": { "type": "string" },
    "maxDate": { "type": "string" },
    "generated": {
      "enum": ["created-at", "updated-at", "deleted-at"]
    }
  }
}
