#!/bin/bash

DIR="./generated/schema.prisma"

if [ -f "$DIR" ]; then

  echo "<%- projectName %> prisma client already exists"

else

  echo "Generating <%- projectName %> prisma client"

  npx prisma migrate dev --name updated

  npx prisma generate
  
fi
